#include<bits/stdc++.h>
using namespace std;
int main ()
{
  double a=100000;
  double b=100000;
  double c= a*b;
  cout<<fixed<<c<<endl;//if we do not use fixed then output is 1e+010;
  return 0;
}